<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link rel="stylesheet" href="css/style.css">
    <style>
        /* עיצוב כללי של הדף */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7; /* צבע רקע כללי */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* עיצוב של תיבת הרישום */
        .signup-container {
            background-color: #ffffff; /* צבע הרקע של התיבה */
            padding: 2rem;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            width: 320px;
            text-align: center;
        }

        .signup-container h2 {
            margin-bottom: 1rem;
            color: #F63C85; /* צבע ורוד */
        }

        /* עיצוב של שדות קלט */
        .signup-container input[type="text"],
        .signup-container input[type="email"],
        .signup-container input[type="password"] {
            width: 100%;
            padding: 0.8rem;
            margin: 0.5rem 0;
            border: 1px solid #c4c4c4;
            border-radius: 4px;
            box-sizing: border-box;
        }

        /* עיצוב של כפתור הרשמה */
        .signup-container .signup-button {
            width: 100%;
            padding: 0.8rem;
            margin-top: 1rem;
            background-color: #A0496C; /* צבע ורוד כהה */
            border: none;
            color: white; /* צבע טקסט לבן */
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .signup-container .signup-button:hover {
            background-color: #c48aa0; /* צבע ורוד בהיר יותר בעת מעבר */
        }

        /* קישור להתחברות */
        .signup-container .login-link {
            display: block;
            margin-top: 1rem;
            font-size: 0.9rem;
            color: #A0496C; /* צבע הקישור */
            text-decoration: none;
        }

        .signup-container .login-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <h2>Create a New Account</h2>
        <form action="welcome.html" method="POST">
            <input type="text" placeholder="Username" required>
            <input type="email" placeholder="Email" required>
            <input type="password" placeholder="Password" required>
            <input type="password" placeholder="Confirm Password" required>
            <button class="signup-button" type="submit">Sign Up</button>
        </form>
        <a class="login-link" href="logIn">Already have an account? Log In</a>
    </div>
</body>
</html>
